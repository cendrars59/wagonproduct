<!--Requirement:Req-Country-001 -->
<!--Title: Edit or Create a new country screen  -->
<!--Description:
code field is in read only sting limited at 6 caracters
label string limited at 40 caracters
Active : check box
Description field is free text zone limited at 255 caracteres
Photo : is possible to add logo
- If the country is tagged as master :
      Label string limited at 40 caracters and in read only mode
      Active check box is disabled
      Master check box is displayed and is disabeled
- If the country is NOT tagged as master :
      Label string limited at 40 caracters and in read only mode
      Active check box is enabled
      Master check box is not displayed and the master country is displayed.
      It is not possible to change the value     -->


<%= simple_form_for @country do |c| %>
  <%= c.input :code, input_html: { maxlength: 3 }%>
  <% if (@country.master == false) %>
    <%= c.input :label, input_html: { maxlength: 40 } %>
    <%= c.input :active %>
    <%= c.association :country, label:'Master',collection: @master,:readonly => true, :label_method => lambda { |country| "#{country.label}" },include_blank: false %>
  <% else %>
    <%= c.input :label, input_html: { maxlength: 40 },:readonly => true%>
    <%= c.input :active, :disabled => true  %>
    <%= c.input :master, label: "Is master", :disabled => true%>
  <% end %>
  <%= c.input :description, as: :text ,input_html: { maxlength: 255 } %>
  <%= c.input :photo, as: :attachinary %>
  <%= c.button :submit, "Validate", class: "btn validate" %>
<% end %>
