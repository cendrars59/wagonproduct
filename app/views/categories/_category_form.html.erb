<%= simple_form_for @category do |c| %>
  <%= c.input :code %>
  <%= c.input :label %>
  <%= c.input :country, collections: @selectable_countries.label, prompt: "Country" %>
  <%= c.input :active %>
  <%= c.button :submit, "Add", class: "btn btn-primary" %>
<% end %>
